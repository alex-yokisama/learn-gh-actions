name: Master Deploy

on:
  push:
    branches:
      - master

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: master

    steps:
    - name: Test output
      run: | 
        echo "TEST_VAR value is ${{ vars.TEST_VAR }}"
        echo "TEST_SECRET value is ${{ secrets.TEST_SECRET }}"
